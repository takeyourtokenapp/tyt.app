# TYT Real-Time Data Integration v2 (NO DEMO MODE)

## Важно

**DEMO MODE УДАЛЕН** - система показывает ТОЛЬКО реальные данные из blockchain и DEX APIs.

## Принцип работы

Система получает реальные данные о токене TYT (`8YuADotEATc86nEgPUZVs8fBRxdMMgEP4JL4xv7rpump`) из нескольких источников в следующем порядке приоритета:

### Иерархия источников данных

1. **Supabase Cache** (< 5 минут)
   - Централизованный кэш в PostgreSQL
   - Быстрая загрузка
   - Доступ для всех пользователей

2. **LocalStorage Cache** (< 1 минуты)
   - Клиентский кэш
   - Мгновенная загрузка
   - Персональный для каждого браузера

3. **Pump.fun Direct API** (Основной источник)
   - `https://frontend-api.pump.fun/coins/{mint}`
   - Самые точные данные
   - Может быть заблокирован CORS

4. **Edge Function Proxy**
   - Pump.fun через Supabase Edge Function
   - Обходит CORS ограничения
   - Требует развертывания

5. **DexScreener API**
   - `https://api.dexscreener.com/latest/dex/tokens/{mint}`
   - Агрегатор DEX данных
   - Публичный API

6. **Jupiter Aggregator**
   - `https://price.jup.ag/v4/price?ids={mint}`
   - Solana price feed
   - Базовые данные цены

7. **Solana RPC** (Fallback)
   - `https://api.mainnet-beta.solana.com`
   - Получение supply с blockchain
   - Всегда доступен

8. **Старый кэш** (> 5 минут)
   - Supabase или localStorage
   - Показывает возраст данных
   - Лучше чем ничего

9. **NO DATA** (нет данных)
   - Показывает "Loading... (retry in 15s)"
   - Статус: желтый, "Connecting..."
   - Автопереподключение

## Визуальные индикаторы

### Зеленый статус (Live Data)
- **Индикатор**: Зеленая пульсирующая точка
- **Баннер**: "Live Market Data" (зеленый фон)
- **Данные**: Реальные данные с DEX/API
- **Источники**: Pump.fun API, DexScreener, Jupiter

### Синий статус (Cached Data)
- **Индикатор**: Синяя пульсирующая точка
- **Баннер**: "Recent Market Data (Cached)" (синий фон)
- **Данные**: Кэшированные реальные данные
- **Возраст**: Показывается в источнике (например "Cached Data (3m old)")

### Желтый статус (Connecting)
- **Индикатор**: Желтая пульсирующая точка
- **Баннер**: "Connecting to Data Sources..." (желтый фон)
- **Данные**: Все нули, идет подключение
- **Источник**: "Loading... (retry in 15s)" или "Error - Retrying..."

### Красный статус (НЕ ДОЛЖЕН ПОЯВЛЯТЬСЯ)
- Красная точка означает критическую ошибку
- Система должна всегда показывать хотя бы "Connecting..."

## База данных: token_price_cache

### Структура таблицы

```sql
CREATE TABLE token_price_cache (
  id uuid PRIMARY KEY,
  token_mint text NOT NULL,
  price numeric,
  market_cap numeric,
  volume_24h numeric,
  price_change_24h numeric,
  holders integer,
  total_supply numeric,
  liquidity numeric,
  source text NOT NULL,
  created_at timestamptz
);
```

### Функции

**`get_latest_token_price(p_token_mint text)`**
- Возвращает последние кэшированные данные
- Включает возраст данных в минутах

**`clean_old_token_cache()`**
- Удаляет старые записи
- Оставляет только последние 100 на токен

### RLS Policies

- **Чтение**: Доступно всем (публичные данные)
- **Запись**: Только authenticated и anon (для edge functions)
- **Обновление/Удаление**: Запрещено (append-only)

## Сценарии работы

### Сценарий 1: Первая загрузка (холодный старт)

1. Проверка Supabase cache → пусто
2. Проверка localStorage → пусто
3. Запрос к Pump.fun → **успех**
4. Сохранение в оба кэша
5. Отображение: ✅ Зеленый "Pump.fun API"

### Сценарий 2: Токен не торгуется

1. Проверка всех кэшей → пусто
2. Pump.fun → нет данных
3. DexScreener → нет данных
4. Jupiter → нет данных
5. Solana RPC → получаем supply
6. Отображение: ✅ Зеленый "Solana Blockchain" (только supply, цена = 0)

### Сценарий 3: Все API недоступны

1. Проверка кэшей → есть данные 10 минут назад
2. Все API недоступны
3. Используем старый кэш
4. Отображение: ✅ Синий "Cached Data (10m old)"

### Сценарий 4: Совсем нет данных

1. Проверка всех источников → нет данных
2. Нет кэша
3. Solana RPC недоступен
4. Отображение: ⚠️ Желтый "Loading... (retry in 15s)"
5. Автоповтор через 15 секунд

## Логирование (Console)

### Успешное получение данных

```javascript
🔍 Fetching REAL TYT token data from multiple sources...
✅ Using recent Supabase cached data
```

или

```javascript
🔍 Fetching REAL TYT token data from multiple sources...
📡 No recent cache, fetching from live sources...
✅ Got REAL data from Pump.fun (direct)
✅ Saved to Supabase cache
```

### Использование fallback

```javascript
🔍 Fetching REAL TYT token data from multiple sources...
📡 No recent cache, fetching from live sources...
Attempting direct Pump.fun API call...
Pump.fun direct API returned: 404
Attempting DexScreener API call...
DexScreener API returned: 200
✅ Got REAL data from DexScreener
```

### Полный fallback до blockchain

```javascript
⚠️ No price data available from exchanges
📊 Attempting to get token supply from Solana RPC...
✅ Got token supply from Solana blockchain
```

### Использование старого кэша

```javascript
⚠️ All live sources failed, checking older cache...
📦 Using older Supabase cache
```

### Нет данных

```javascript
❌ No data available from any source
// Возвращает: source: "Loading... (retry in 15s)"
```

## API Rate Limits

### Безопасные лимиты

| API | Лимит | Наш интервал | Запросов/час |
|-----|-------|--------------|--------------|
| Pump.fun | Неизвестно | 15 сек | 240 |
| DexScreener | 300/мин | 15 сек | 240 |
| Jupiter | Неизвестно | 15 сек | 240 |
| Solana RPC | Нет | По требованию | < 50 |

**Кэширование снижает нагрузку:**
- Supabase cache: 5 минут = 12 запросов/час
- localStorage: 1 минута = 60 запросов/час

## Развертывание

### 1. База данных (автоматически)

Миграция `create_token_price_cache` уже применена к вашей Supabase БД.

### 2. Edge Function (опционально)

```bash
# В Supabase Dashboard:
# 1. Edge Functions → Deploy New Function
# 2. Name: fetch-tyt-price
# 3. Копируем: /supabase/functions/fetch-tyt-price/index.ts
# 4. Deploy
```

### 3. Переменные окружения (уже настроены)

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

## Мониторинг

### Проверка работы системы

**Открыть консоль браузера (F12):**
- Должны быть логи `🔍 Fetching REAL TYT token data...`
- Проверить какой источник используется
- Убедиться что нет ошибок

**На странице TYT Trading:**
- Проверить цвет индикатора (зеленый/синий/желтый)
- Проверить название источника
- Проверить время последнего обновления

**В Supabase Dashboard:**
```sql
-- Проверить кэш
SELECT * FROM token_price_cache
WHERE token_mint = '8YuADotEATc86nEgPUZVs8fBRxdMMgEP4JL4xv7rpump'
ORDER BY created_at DESC
LIMIT 10;

-- Проверить возраст данных
SELECT *,
  EXTRACT(EPOCH FROM (now() - created_at))::integer / 60 as age_minutes
FROM token_price_cache
WHERE token_mint = '8YuADotEATc86nEgPUZVs8fBRxdMMgEP4JL4xv7rpump'
ORDER BY created_at DESC
LIMIT 1;
```

## Troubleshooting

### Проблема: Всегда показывает "Loading..."

**Причина:** Токен не торгуется ни на одной бирже

**Решение:**
1. Проверить токен на Pump.fun: https://pump.fun/coin/8YuADot...
2. Подождать пока токен появится на DEX
3. Система должна получить хотя бы supply с Solana RPC

### Проблема: Данные не обновляются

**Причина:** Кэш блокирует обновление

**Решение:**
1. Нажать "Refresh" вручную
2. Очистить localStorage: `localStorage.removeItem('tyt_token_cache')`
3. Очистить Supabase кэш: `DELETE FROM token_price_cache WHERE ...`

### Проблема: Показывает очень старые данные

**Причина:** Все API недоступны, используется старый кэш

**Решение:**
1. Проверить интернет-соединение
2. Проверить доступность API в другом браузере
3. Подождать пока API восстановятся

## Отличия от Demo Mode (v1)

| Аспект | Demo Mode v1 | Real Data v2 |
|--------|--------------|--------------|
| Данные | Симуляция | Реальные |
| Fallback | Случайные числа | Старый кэш или "Loading..." |
| Цвет "нет данных" | Синий (позитивный) | Желтый (ожидание) |
| Индикация | "Demo Mode" | "Loading..." / "Cached Data" |
| База данных | Нет | token_price_cache |
| Для пользователей | Вводит в заблуждение | Честная информация |

## Заключение

Система **ВСЕГДА** показывает только реальные данные:
- ✅ Live data с API
- ✅ Cached real data (с указанием возраста)
- ✅ Blockchain data (supply)
- ⚠️ "Loading..." (если нет данных пока)
- ❌ **НЕТ симуляции/демо режима**

Пользователи всегда видят правду о состоянии токена TYT.

---

**Дата:** 2026-01-09
**Версия:** 2.0 (Real Data Only)
**Статус:** Production Ready ✅
