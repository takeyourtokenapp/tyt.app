# 🌍 Мультиязычность TYT - Быстрый Старт

**Статус:** ✅ Полностью работает
**Языки:** English 🇬🇧 | Русский 🇷🇺 | עברית 🇮🇱

---

## ✅ Что Реализовано

### 🎯 Автоматическое Определение Языка

**3 уровня определения:**
1. **Сохраненные настройки** - если пользователь выбирал язык ранее
2. **Геолокация** - определение по IP адресу (ipapi.co)
3. **Браузер** - `navigator.language`

**Географическая привязка:**
```
🇷🇺 Россия, Беларусь, Казахстан → Русский
🇮🇱 Израиль → Иврит (עברית)
🇺🇸 США, UK, и др. → English
```

### 🎨 Полная Поддержка RTL для Иврита

- Автоматическая установка `dir="rtl"`
- Зеркальное отображение интерфейса
- Правильное выравнивание текста справа налево
- Иконки отзеркаливаются где нужно

### 🔧 Компоненты

**Готовые компоненты:**
- `LanguageSelector` - переключатель языка с флагами
- `LanguageProvider` - контекст управления языком
- Автоматическая система переводов

---

## 🚀 Как Использовать

### 1. В Любом Компоненте

```typescript
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();

  return (
    <div>
      <h1>{t('common:app.name')}</h1>
      <button>{t('common:common.save')}</button>
      <p>{t('common:wallet.balance')}: {balance}</p>
    </div>
  );
}
```

### 2. Доступ к Контексту Языка

```typescript
import { useLanguage } from '../contexts/LanguageContext';

function MyComponent() {
  const { currentLanguage, changeLanguage, isRTL } = useLanguage();

  return (
    <div>
      <p>Текущий язык: {currentLanguage}</p>
      <button onClick={() => changeLanguage('ru')}>
        Переключить на русский
      </button>
      {isRTL && <p>RTL mode активен</p>}
    </div>
  );
}
```

---

## 📝 Добавление Переводов

### Шаг 1: Добавьте ключ во все языковые файлы

**English** (`src/locales/en/common.json`):
```json
{
  "myFeature": {
    "title": "My Feature",
    "description": "Description"
  }
}
```

**Russian** (`src/locales/ru/common.json`):
```json
{
  "myFeature": {
    "title": "Моя Функция",
    "description": "Описание"
  }
}
```

**Hebrew** (`src/locales/he/common.json`):
```json
{
  "myFeature": {
    "title": "התכונה שלי",
    "description": "תיאור"
  }
}
```

### Шаг 2: Используйте в компоненте

```typescript
const { t } = useTranslation();
<h2>{t('common:myFeature.title')}</h2>
```

---

## 📁 Структура Файлов

```
src/
├── i18n/
│   └── config.ts                    # ⚙️ Конфигурация i18n
├── locales/
│   ├── en/common.json               # 🇬🇧 English
│   ├── ru/common.json               # 🇷🇺 Русский
│   └── he/common.json               # 🇮🇱 עברית
├── contexts/
│   └── LanguageContext.tsx          # 🌍 Контекст языка
├── components/
│   └── LanguageSelector.tsx         # 🎛️ Переключатель
└── utils/
    └── languageDetector.ts          # 🔍 Определение языка
```

---

## 🔑 Ключевые Функции

### Определение Языка

```typescript
import { detectInitialLanguage } from './utils/languageDetector';

const lang = await detectInitialLanguage();
// Вернет: 'en' | 'ru' | 'he'
```

### Смена Языка

```typescript
import { useLanguage } from './contexts/LanguageContext';

const { changeLanguage } = useLanguage();
await changeLanguage('ru');
```

### Проверка RTL

```typescript
import { useLanguage } from './contexts/LanguageContext';

const { isRTL } = useLanguage();
if (isRTL) {
  // Особая логика для RTL
}
```

---

## 🎭 RTL (Иврит)

### Автоматически Применяется

При выборе иврита:
- `<html dir="rtl">` установлен
- `<body class="rtl">` добавлен
- Layout зеркально отображается

### CSS Утилиты для RTL

```css
/* Автоматически применяются */
[dir="rtl"] .rtl\:text-right { text-align: right; }
[dir="rtl"] .rtl\:mirror { transform: scaleX(-1); }
```

### Использование в Tailwind

```tsx
<div className="text-left rtl:text-right">
  Текст автоматически выровнен правильно
</div>
```

---

## ✅ Чеклист для Разработчиков

При добавлении новых функций:

- [ ] Добавьте перевод на английский (`en/common.json`)
- [ ] Добавьте перевод на русский (`ru/common.json`)
- [ ] Добавьте перевод на иврит (`he/common.json`)
- [ ] Используйте `t()` вместо хардкод текста
- [ ] Протестируйте со всеми 3 языками
- [ ] Проверьте RTL layout для иврита
- [ ] Убедитесь что текст помещается в UI

---

## 🧪 Тестирование

### Ручная Смена Языка

```javascript
// В консоли браузера
import i18n from './i18n/config';

// Переключить на русский
i18n.changeLanguage('ru');

// Переключить на иврит
i18n.changeLanguage('he');

// Переключить на английский
i18n.changeLanguage('en');
```

### Очистка Сохраненного Языка

```javascript
// Удалить сохраненный выбор
localStorage.removeItem('tyt_language');

// Перезагрузить страницу
location.reload();
```

---

## 📊 Текущие Переводы

### Доступные Секции

- ✅ `app` - Название, слоган, описание
- ✅ `nav` - Навигация, меню
- ✅ `common` - Общие кнопки, действия
- ✅ `wallet` - Кошелек, транзакции
- ✅ `mining` - Майнинг, хешрейт
- ✅ `marketplace` - Маркетплейс NFT
- ✅ `academy` - Академия, обучение
- ✅ `foundation` - Фонд, миссия
- ✅ `auth` - Авторизация, регистрация
- ✅ `errors` - Ошибки валидации
- ✅ `success` - Успешные сообщения

**Всего ключей:** ~100 переводов × 3 языка = 300 строк

---

## 🐛 Решение Проблем

### Язык не меняется?

1. Проверьте `LanguageProvider` в `main.tsx`
2. Убедитесь `i18n/config` импортирован
3. Очистите localStorage

### Переводы не работают?

1. Проверьте что ключ есть во ВСЕХ файлах
2. Используйте правильный namespace: `t('common:key')`
3. Проверьте валидность JSON

### RTL не работает для иврита?

1. Язык установлен на `he`?
2. Проверьте `dir="rtl"` на `<html>`
3. RTL стили в `index.css` подключены?

---

## 📚 Полная Документация

Детальное руководство: [`docs/MULTILINGUAL_GUIDE.md`](docs/MULTILINGUAL_GUIDE.md)

**Содержит:**
- Архитектура системы
- Подробные примеры кода
- API документация
- Продвинутые техники
- Планы развития

---

## 🎉 Готово к Использованию!

Система мультиязычности **полностью работает** и готова к продакшн.

**Автоматически определяет язык → Пользователь может выбрать → Сохраняется навсегда**

---

**Обновлено:** 26 декабря 2024
**Версия:** 1.0.0
**Статус:** ✅ Production Ready
