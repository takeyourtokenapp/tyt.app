# 📁 Структура Папок TYT

**Чтобы не запутаться:**

---

## 🗂️ Две Папки - Две Функции

### 1️⃣ Desktop/tyt.app

**Назначение:** Архивы из bolt.new (для хранения)

**Статус:** НЕ git репозиторий

**Функция:**
- Просто хранилище скачанных архивов
- Можно просматривать файлы
- НЕ связано с GitHub

**Что там:**
```
~/Desktop/tyt.app/
├── все файлы проекта
└── (нет .git)
```

**Использование:**
- Скачали архив → распаковали на Desktop
- Посмотрели что внутри
- Всё

---

### 2️⃣ Downloads/tyt.app

**Назначение:** Git репозиторий, связанный с GitHub

**Статус:** Git репозиторий (есть .git)

**Функция:**
- Рабочая копия проекта
- Связан с GitHub
- Все изменения через git commit/push

**Что там:**
```
~/Downloads/tyt.app/
├── .git/           (связь с GitHub)
├── .env            (ваши секреты)
└── все файлы проекта
```

**Использование:**
- Получает обновления из архивов
- Делает commit
- Push на GitHub

---

## 🔄 Workflow

```
1. bolt.new → Download
      ↓
   ~/Downloads/tyt-app-latest.zip

2. Распаковка архива → ~/Desktop/tyt.app (опционально)
      ↓
   Можете посмотреть что внутри

3. Скрипт ./sync-to-github-repo.sh
      ↓
   Копирует в ~/Downloads/tyt.app

4. Git операции
      ↓
   git commit → git push → GitHub
```

---

## ✅ Правильное Использование

### Когда работаете с проектом:

```bash
cd ~/Downloads/tyt.app    # ✅ ВСЕГДА здесь
git status
npm run build
git commit
git push
```

### Когда синхронизируете из bolt.new:

```bash
# 1. Скачали архив
cd ~/Downloads/tyt-app-latest

# 2. Запустили скрипт
./sync-to-github-repo.sh

# Скрипт автоматически:
# - Берет файлы из архива
# - Копирует в ~/Downloads/tyt.app
# - Делает commit и push
```

---

## ❌ Частые Ошибки

### Ошибка 1: Работать в Desktop/tyt.app

```bash
cd ~/Desktop/tyt.app
git status    # ❌ Ошибка: not a git repository
```

**Решение:** Работайте в `~/Downloads/tyt.app`

### Ошибка 2: Копировать файлы вручную

```bash
cp ~/Desktop/tyt.app/* ~/Downloads/tyt.app/   # ❌ Плохо
```

**Решение:** Используйте скрипт `./sync-to-github-repo.sh`

### Ошибка 3: Push из неправильной папки

```bash
cd ~/Desktop/tyt.app
git push    # ❌ Не сработает
```

**Решение:** Работайте в `~/Downloads/tyt.app`

---

## 🎯 Быстрая Шпаргалка

| Задача | Команда |
|--------|---------|
| Синхронизация из bolt.new | `cd ~/Downloads/tyt-app-latest && ./sync-to-github-repo.sh` |
| Проверить статус | `cd ~/Downloads/tyt.app && git status` |
| Сделать commit | `cd ~/Downloads/tyt.app && git add . && git commit -m "update"` |
| Push на GitHub | `cd ~/Downloads/tyt.app && git push origin main` |
| Build проекта | `cd ~/Downloads/tyt.app && npm run build` |
| Посмотреть GitHub | `open https://github.com/takeyourtokenapp/tyt.app` |

---

## 🔍 Как Проверить Что Где

### Проверить Desktop/tyt.app:

```bash
cd ~/Desktop/tyt.app
ls -la .git    # Если ошибка - значит НЕ git (правильно)
```

### Проверить Downloads/tyt.app:

```bash
cd ~/Downloads/tyt.app
ls -la .git    # Должна быть папка .git (правильно)
git remote -v  # Должен показать GitHub
```

---

## 📊 Резюме

| Папка | Git? | Связь с GitHub | Назначение |
|-------|------|----------------|------------|
| `~/Desktop/tyt.app` | ❌ Нет | ❌ Нет | Хранилище архивов |
| `~/Downloads/tyt.app` | ✅ Да | ✅ Да | Рабочий репозиторий |

**Запомните:**
- Desktop = просмотр
- Downloads = работа с git

---

**Не запутаетесь! 🎯**
