# 🔍 Сайт не показывается - Диагностика

## Что делать если не видите сайт:

### 1️⃣ Проверьте запущен ли dev сервер

Dev сервер запускается АВТОМАТИЧЕСКИ системой.
Вы должны видеть в терминале что-то вроде:
```
VITE v5.x.x  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**Если не видите** - сервер не запущен.

---

### 2️⃣ Откройте тестовую страницу

Попробуйте открыть:
```
http://localhost:5173/test.html
```

**Если видите тестовую страницу** - сборка работает, проблема в приложении.
**Если НЕ видите** - проблема с сервером или портом.

---

### 3️⃣ Откройте консоль браузера

1. **Нажмите F12** (или Cmd+Option+I на Mac)
2. **Перейдите на вкладку Console**
3. **Обновите страницу** (F5 или Cmd+R)
4. **Посмотрите на ошибки** (красный текст)

#### Типичные ошибки:

**❌ "Failed to fetch dynamically imported module"**
→ Очистите кэш (Ctrl+Shift+Delete) и перезагрузите

**❌ "Supabase client error"**
→ Проблема с .env файлом или Supabase

**❌ "Cannot read properties of undefined"**
→ Ошибка в коде, проверьте консоль

**❌ Белый экран без ошибок**
→ Проблема с ThemeProvider или контекстом

---

### 4️⃣ Проверьте порт

Убедитесь что открываете правильный URL:
```
http://localhost:5173/         ✅ Правильно
http://localhost:5174/         ❌ Неправильный порт
http://localhost:3000/         ❌ Неправильный порт
```

---

### 5️⃣ Проверьте сборку

Выполните команду:
```bash
npm run build
```

Должны увидеть:
```
✓ built in XX.XXs
```

Если есть ошибки (errors) - сообщите о них.

---

### 6️⃣ Проверьте файл .env

Файл `.env` должен содержать:
```
VITE_SUPABASE_URL=https://xyvzpezqavqujpxodtre.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

**Если файл пустой или другие значения** - проблема с окружением.

---

### 7️⃣ Попробуйте другие страницы

Откройте:
- http://localhost:5173/login
- http://localhost:5173/about
- http://localhost:5173/test-load

**Если хотя бы одна открывается** - проблема на конкретной странице.

---

## 🛠️ Быстрые исправления

### Очистить кэш и перезагрузить:
```bash
# В браузере:
Ctrl + Shift + Delete → Очистить кэш → OK
Ctrl + F5 (принудительная перезагрузка)
```

### Перезапустить сервер:
```bash
# Остановить: Ctrl + C
# Запустить:
npm run dev
```

### Проверить что порт свободен:
```bash
# Linux/Mac:
lsof -i :5173

# Windows:
netstat -ano | findstr :5173
```

---

## 📋 Что сообщить для помощи:

1. **Что видите на экране:**
   - Белый экран
   - Ошибка 404
   - Ошибка с текстом (какой?)
   - Ничего не загружается

2. **Ошибки из консоли:**
   - Скопируйте текст ошибок (красный текст)
   - Сделайте скриншот консоли

3. **Какой URL открываете:**
   - http://localhost:5173/
   - Другой?

4. **Открывается ли test.html:**
   - http://localhost:5173/test.html
   - Да/Нет

5. **Результат npm run build:**
   - ✓ built успешно
   - errors (какие?)

---

## ✅ Текущий статус проекта:

- ✅ Сборка успешна (17.54s)
- ✅ Файлы скомпилированы в dist/
- ✅ .env файл настроен
- ✅ Все зависимости установлены
- ✅ Нет ошибок TypeScript
- ✅ Код валиден

**Проблема скорее всего:**
- Dev сервер не запущен
- Неправильный порт
- Кэш браузера
- Ошибка в рантайме (проверьте консоль)

---

