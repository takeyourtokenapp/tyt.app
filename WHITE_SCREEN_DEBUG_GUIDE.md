# 🔍 TYT White Screen Debugging Guide

## Проблема
После внесения изменений приложение показывает белый экран.

---

## ✅ Исправления, которые уже применены:

### 1. i18n Suspense Issue
**Проблема:** React i18n блокировал рендер в Suspense режиме
**Решение:** Отключён `useSuspense` в `src/i18n/config.ts`
```typescript
react: {
  useSuspense: false, // ✅ Fixed
}
```

### 2. Циклическая зависимость Context
**Проблема:** `AoiControlProvider` вызывал `useAoi()`, создавая циклическую зависимость
**Решение:** Перенесена функция `logInteraction` внутрь `AoiControlProvider`
**Файл:** `src/contexts/AoiControlContext.tsx`

### 3. TypeScript ошибки
**Проблема:** Неиспользуемые импорты блокировали сборку
**Решение:** Удалены неиспользуемые импорты из `AoiChatWidget.tsx`

---

## 🧪 Диагностика (Шаг за шагом)

### Шаг 1: Проверьте тестовую страницу
Откройте в браузере:
```
http://localhost:5173/test-load
```

Эта страница покажет:
- ✅ Статус инициализации компонентов
- ✅ Готовность i18n
- ✅ Статус аутентификации
- ✅ Переменные окружения

### Шаг 2: Проверьте консоль браузера
Откройте DevTools (F12) и проверьте вкладку Console на наличие ошибок:

**Хорошие сигналы:**
```
✅ i18n initialized with language: en
✅ Auth state changed: SIGNED_OUT
✅ Foundation API status check...
```

**Плохие сигналы:**
```
❌ Uncaught Error: ...
❌ Failed to load module...
❌ Cannot read property 'XXX' of undefined
```

### Шаг 3: Проверьте переменные окружения
Убедитесь, что файл `.env` существует и содержит:

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_anon_key
```

**ВАЖНО:** После изменения `.env` файла нужно перезапустить dev сервер!

### Шаг 4: Проверьте Network вкладку
В DevTools → Network проверьте:
- ✅ Все запросы к `/assets/*.js` загружаются (200 OK)
- ✅ Запрос к Supabase успешен
- ❌ Ищите 404 или 500 ошибки

---

## 🛠️ Решения распространённых проблем

### Проблема 1: "Cannot read property of undefined"
**Причина:** Неинициализированный контекст
**Решение:**
1. Проверьте порядок провайдеров в `src/main.tsx`
2. Убедитесь, что все провайдеры обернуты правильно

### Проблема 2: "Module not found"
**Причина:** Отсутствует зависимость или неправильный импорт
**Решение:**
```bash
npm install
npm run build
```

### Проблема 3: "Failed to fetch"
**Причина:** Неправильный Supabase URL или ключ
**Решение:**
1. Проверьте `.env` файл
2. Убедитесь, что Supabase проект активен
3. Проверьте правильность ключей

### Проблема 4: Белый экран без ошибок
**Причина:** CSS или роутинг проблема
**Решение:**
1. Очистите кэш браузера (Ctrl+Shift+Delete)
2. Попробуйте другой браузер
3. Проверьте `index.html` - должен быть `<div id="root"></div>`

---

## 📊 Текущая Архитектура Провайдеров

```
<StrictMode>
  <Suspense>                          ← Fallback для ленивой загрузки
    <ThemeProvider>                   ← Темная/светлая тема
      <LanguageProvider>              ← Мультиязычность (EN/RU/HE)
        <WagmiWeb3Provider>           ← Web3 Wagmi конфиг
          <BrowserRouter>             ← React Router
            <AuthProvider>            ← Supabase аутентификация
              <Web3Provider>          ← Web3 контракты
                <MultiChainWeb3Provider> ← Мультичейн поддержка
                  <ToastProvider>     ← Уведомления
                    <AoiProvider>     ← aOi AI контекст
                      <AoiControlProvider> ← aOi контроль платформы
                        <App />       ← Основное приложение
```

---

## 🔧 Команды для отладки

### Полная пересборка:
```bash
# 1. Очистить кэш
rm -rf node_modules/.vite
rm -rf dist

# 2. Пересобрать
npm run build

# 3. Проверить типы
npm run typecheck
```

### Проверка кода:
```bash
# Lint check
npm run lint
```

### Локальный запуск:
```bash
# Dev сервер (автоматически запускается)
npm run dev

# Предпросмотр production билда
npm run preview
```

---

## 📝 Чек-лист отладки

Проверьте следующее по порядку:

- [ ] `.env` файл существует и заполнен
- [ ] `npm install` выполнена успешно
- [ ] `npm run build` проходит без ошибок
- [ ] `/test-load` страница загружается
- [ ] Консоль браузера не показывает ошибок
- [ ] Network вкладка показывает успешные загрузки
- [ ] Кэш браузера очищен
- [ ] Dev сервер перезапущен после изменений `.env`

---

## 🆘 Если ничего не помогло

### Вариант 1: Минимальная загрузка
Временно упростите `src/App.tsx`:

```tsx
export default function App() {
  return (
    <div style={{ padding: '20px', background: '#1a1a1a', color: 'white', minHeight: '100vh' }}>
      <h1>✅ App Loading Works!</h1>
      <p>If you see this, basic React works.</p>
    </div>
  );
}
```

Если это работает, проблема в одном из провайдеров.

### Вариант 2: Поэтапное подключение
Добавляйте провайдеры по одному, проверяя после каждого:
1. Только ErrorBoundary
2. + ThemeProvider
3. + LanguageProvider
4. + AuthProvider
5. И так далее...

### Вариант 3: Проверка Supabase подключения
```bash
# Откройте в браузере
http://localhost:5173/test-supabase

# Должно показать статус подключения к Supabase
```

---

## 📞 Дополнительная информация

### Логи которые НЕ являются ошибками:
```
⚠️ Foundation API unavailable, using local fallback
⚠️ Some chunks are larger than 500 kB
```
Эти предупреждения нормальны и не ломают приложение.

### Критические ошибки:
```
❌ Cannot find module
❌ Uncaught Error in promise
❌ Failed to compile
❌ TypeError: Cannot read property
```
Эти ошибки требуют исправления.

---

## 🎯 Следующие шаги после исправления

1. Закройте все вкладки с приложением
2. Очистите кэш браузера
3. Перезапустите dev сервер
4. Откройте приложение в новой вкладке
5. Проверьте `/test-load` для диагностики
6. Если всё работает, переходите на главную страницу `/`

---

**Создано:** 2025-12-28
**Статус билда:** ✅ Успешно
**Последняя проверка:** Все критические исправления применены
