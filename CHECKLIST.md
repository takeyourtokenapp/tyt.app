# TYT Ecosystem - Checklist для запуска

## 📋 Обязательные шаги перед запуском

### 1. ⚙️ Установка зависимостей

- [ ] **Установить Node.js пакеты**
  ```bash
  npm install
  ```

- [ ] **Установить Solana Web3**
  ```bash
  npm install @solana/web3.js@1.87.6 --legacy-peer-deps
  ```
  Если не получается, попробуйте:
  ```bash
  rm -rf node_modules package-lock.json
  npm install
  npm install @solana/web3.js@1.87.6
  ```

### 2. 🔐 Настройка секретов

- [ ] **Сгенерировать секретные ключи**
  ```bash
  bash generate-secrets.sh
  ```

- [ ] **Скопировать сгенерированные значения в .env файл**
  - Заменить `WEBHOOK_SECRET`
  - Заменить `WALLET_ENCRYPTION_KEY`
  - Заменить `CRON_SECRET`

### 3. 🌐 Настройка внешних сервисов

- [ ] **Получить TRONGRID_API_KEY**
  1. Перейти на https://www.trongrid.io/
  2. Зарегистрироваться/войти
  3. Создать API key
  4. Скопировать в .env файл

- [ ] **Получить адрес TYT токена с pump.fun**
  1. Открыть pump.fun где размещен токен TYT
  2. Скопировать Contract Address
  3. Вставить в .env как `VITE_TYT_TOKEN_MINT`

- [ ] **Обновить адрес токена в коде**
  - Открыть `src/utils/pumpFun.ts`
  - Заменить `TYT_PUMP_FUN_ADDRESS_HERE` на реальный адрес

### 4. 🗄️ База данных

- [ ] **Проверить, что все миграции применены**
  - Зайти в Supabase Dashboard
  - Перейти в Database → Migrations
  - Убедиться, что все миграции выполнены (статус: Completed)

- [ ] **Проверить RLS политики**
  - Database → Tables
  - Каждая таблица должна иметь "RLS enabled"
  - Проверить наличие policies

- [ ] **Проверить функции**
  - Database → Functions
  - Должна быть функция `get_user_tyt_holdings`

### 5. ⚡ Edge Functions

- [ ] **Задеплоить все Edge Functions**
  Через Supabase Dashboard или CLI:
  ```bash
  supabase functions deploy process-payment
  supabase functions deploy process-deposit
  supabase functions deploy blockchain-webhook
  supabase functions deploy monitor-deposits
  supabase functions deploy generate-deposit-address
  ```

- [ ] **Проверить статус функций**
  - Functions → Edge Functions
  - Все функции должны быть "Deployed"

### 6. 🏗️ Сборка проекта

- [ ] **Запустить typecheck**
  ```bash
  npm run typecheck
  ```

- [ ] **Собрать проект**
  ```bash
  npm run build
  ```

- [ ] **Убедиться, что сборка прошла без ошибок**

### 7. 🧪 Тестирование

- [ ] **Запустить dev сервер локально**
  ```bash
  npm run dev
  ```

- [ ] **Проверить основные функции:**
  - Регистрация нового пользователя
  - Вход в систему
  - Навигация по страницам

- [ ] **Проверить Web3 интеграцию:**
  - Установить Phantom Wallet (https://phantom.app/)
  - Подключить кошелек на странице TYT Trading
  - Проверить отображение адреса кошелька

- [ ] **Проверить подключение к базе данных:**
  - Создать тестовую запись
  - Проверить, что данные сохраняются

### 8. 🚀 Production деплой

- [ ] **Пересобрать с production переменными**
  ```bash
  npm run build
  ```

- [ ] **Загрузить на Hostinger**
  - Подключиться через FTP/SFTP
  - Загрузить содержимое папки `dist/` в `public_html/`
  - Убедиться, что `.htaccess` скопирован

- [ ] **Проверить сайт в production**
  - Открыть ваш домен
  - Проверить все основные функции
  - Проверить HTTPS работает

### 9. 🔒 Безопасность

- [ ] **Убедиться, что .env не в git**
  ```bash
  cat .gitignore | grep .env
  ```

- [ ] **Включить 2FA на Supabase**

- [ ] **Настроить Rate Limiting** (опционально)

- [ ] **Проверить CORS настройки Edge Functions**

### 10. 📊 Мониторинг

- [ ] **Настроить алерты в Supabase**
  - Database health
  - API usage
  - Error rates

- [ ] **Добавить Sentry для ошибок** (опционально)

---

## ⚡ Быстрый старт (минимальная конфигурация)

Для быстрого запуска нужны только 3 шага:

### Шаг 1: Секреты
```bash
bash generate-secrets.sh
# Скопировать вывод в .env
```

### Шаг 2: TYT токен
- Получить адрес с pump.fun
- Добавить в .env как `VITE_TYT_TOKEN_MINT`

### Шаг 3: Сборка
```bash
npm install @solana/web3.js@1.87.6
npm run build
```

Готово! Система готова к работе.

---

## 🐛 Решение проблем

### Проблема: npm install падает с ошибкой памяти
**Решение:**
```bash
export NODE_OPTIONS="--max-old-space-size=4096"
npm install --legacy-peer-deps
```

### Проблема: Phantom не подключается
**Решение:**
1. Убедитесь, что расширение установлено
2. Обновите страницу (F5)
3. Откройте DevTools и проверьте: `console.log(window.solana)`
4. Проверьте, что сайт имеет разрешения в Phantom Settings

### Проблема: Edge Functions не работают
**Решение:**
1. Проверьте статус в Supabase Dashboard
2. Проверьте логи функций
3. Убедитесь, что CORS headers настроены
4. Перезадеплойте функцию

### Проблема: RLS блокирует запросы
**Решение:**
1. Проверьте, что пользователь авторизован
2. Проверьте RLS policies в таблицах
3. Проверьте `auth.uid()` в policies

---

## 📝 Полезные команды

```bash
# Проверка типов
npm run typecheck

# Сборка
npm run build

# Dev сервер
npm run dev

# Генерация секретов
bash generate-secrets.sh

# Очистка node_modules
rm -rf node_modules package-lock.json && npm install
```

---

## 🎯 Приоритеты

### Критично (без этого не работает):
1. ✅ Supabase настроен
2. ⚠️ Секреты сгенерированы (WEBHOOK_SECRET, WALLET_ENCRYPTION_KEY, CRON_SECRET)
3. ⚠️ TYT токен адрес добавлен

### Важно (для полной функциональности):
4. ⚠️ TRONGRID_API_KEY получен
5. ⚠️ Edge Functions задеплоены
6. ⚠️ @solana/web3.js установлен

### Опционально (для улучшения):
7. Sentry для мониторинга
8. Helius RPC для Solana
9. Rate limiting
10. Analytics

---

## ✅ Готовность к запуску

Система готова к запуску когда:
- [x] Supabase настроен
- [ ] Все секреты заполнены в .env
- [ ] TYT токен адрес добавлен
- [ ] @solana/web3.js установлен
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Edge Functions задеплоены

**Текущий статус:** 1/6 (16%)

После выполнения всех шагов система будет полностью готова к работе!
